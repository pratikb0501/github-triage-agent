services:
  triage-agent:
    build: .
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    stdin_open: true
    tty: true